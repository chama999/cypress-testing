describe('Cuentas', function() {
    var a = 1+1
    var b = 2
    it('Probemos si funciona bien', () => {
        expect(a==b).to.be.true;
    });
    it('Probemos si funciona bien2', () => {
        expect(a-b).to.equal(0);
    });
})
